import Router from './routes';

/**
 * Main App component
 * Integrates the new router with all public and protected routes
 * Uses React Router v7 with createBrowserRouter
 */
function App() {
  return <Router />;
}

export default App;
